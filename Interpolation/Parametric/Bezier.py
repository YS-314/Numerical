import numpy as np
import math
def Bernstein(i,n,u):
  return math.comb(n,i)*math.pow(u,i)*math.pow(1-u,n-i)

def BP_C(t,deg,dim):
  for n in range()

def Bezier(P,t,deg,dim): # dim = P.ndim, which is length of t (a vector)
  BPcf = 
