



class u_xi:
    def __init__(ui,ist, iend,delta):
        uio.ist = ist
        uio.iend = iend
        uio.delta = delta
    def ui(uio, i):
        return i*uio.delta

class u_xi2:
    def __init__(ui,start,delta,i):
        ui.start = start
        ui.delta = delta
        ui.ir 


#Non uniform grid 
class nu_xi:
